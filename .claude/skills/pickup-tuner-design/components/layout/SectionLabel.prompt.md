Spaced-uppercase dim caption that titles a functional block; usually rendered for you by `Panel`'s `label` prop, exported here for hand-composed headers.

```jsx
<SectionLabel>Capture grid</SectionLabel>
```
